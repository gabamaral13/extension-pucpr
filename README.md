# extension-pucpr
instalar essas dependecias pro back end 

npm init -y
npm install express sqlite3 bcrypt jsonwebtoken cors

parte do npm acima instalado ok npm init -y e os npm install express sqlite3 bcrypt jsonwebtoken cors (linha 4 e 5 instalados)